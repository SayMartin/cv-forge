// Only the language section for now — the rest of the settings page is
// translated in a later step, and an empty slice would be worse than a partial
// one. The heading is here because the section owns it.
export const settings = {
  language: {
    title: "Language",
    description:
      "Applies to this account, on every device you sign in from. It does not change the language your CVs are written in — that is set per CV.",
    currentBadge: "current",
  },
};
